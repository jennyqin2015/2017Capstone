{% extends "base.jinja2" %}
{% block content %}
    <div class="jumbotron">
        <h1 class="display-3">Basic MVO</h1>
        <p>Given a choice between three risk appetites, this application runs a simple Mean variance Optimization and
            returns the optimal portfolio. Note: All portfolios contain the same 5 assets which can be found under
            <strong>Stocks</strong> in the navigation bar above.</p>
        <p>Just sign up, set your risk appetite, and we'll do the rest!</p>
        {% if session['email'] %}
            <p class="lead">
                <a class="btn btn-primary btn-lg" href="{{ url_for('users.logout_user') }}" role="button">Log Out</a>
            </p>
        {% else %}
            <p class="lead">
                <a class="btn btn-primary btn-lg" href="{{ url_for('users.register_user') }}" role="button">Sign up</a>
            </p>
        {% endif %}
    </div>
{% endblock %}
